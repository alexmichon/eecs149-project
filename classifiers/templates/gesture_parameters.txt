#ifndef GESTURE_PARAMETERS_H
#define GESTURE_PARAMETERS_H

#define GESTURE_CLASSES $GESTURE_CLASSES
#define GESTURE_FEATURES $GESTURE_FEATURES
#define GESTURE_DIMENSIONS $GESTURE_DIMENSIONS

const float GESTURE_XBAR[GESTURE_FEATURES] = $GESTURE_XBAR;
const float GESTURE_SCALINGS[GESTURE_FEATURES][GESTURE_DIMENSIONS] = $GESTURE_SCALINGS;

const float GESTURE_GNB_CLASS_PRIOR[GESTURE_CLASSES] = $GESTURE_GNB_CLASS_PRIOR;
const float GESTURE_GNB_MEANS[GESTURE_CLASSES][GESTURE_DIMENSIONS] = $GESTURE_GNB_MEANS;
const float GESTURE_GNB_STDEVS[GESTURE_CLASSES][GESTURE_DIMENSIONS] = $GESTURE_GNB_STDEVS;

#endif
#ifndef MODE_PARAMETERS_H
#define MODE_PARAMETERS_H

#define MODE_CLASSES $MODE_CLASSES
#define MODE_FEATURES $MODE_FEATURES
#define MODE_DIMENSIONS $MODE_DIMENSIONS
#define MODE_WINDOW $MODE_WINDOW

const float MODE_XBAR[MODE_FEATURES] = $MODE_XBAR;
const float MODE_SCALINGS[MODE_FEATURES][MODE_DIMENSIONS] = $MODE_SCALINGS;

const float MODE_GNB_CLASS_PRIOR[MODE_CLASSES] = $MODE_GNB_CLASS_PRIOR;
const float MODE_GNB_MEANS[MODE_CLASSES][MODE_DIMENSIONS] = $MODE_GNB_MEANS;
const float MODE_GNB_STDEVS[MODE_CLASSES][MODE_DIMENSIONS] = $MODE_GNB_STDEVS;

#endif
#ifndef GAUSSIAN_NB_PARAMETERS_H
#define GAUSSIAN_NB_PARAMETERS_H

#include "LDA_Parameters.h"

const float GNB_CLASS_PRIOR[LDA_CLASSES] = $GNB_CLASS_PRIOR;
const float GNB_MEANS[LDA_CLASSES][LDA_DIMENSIONS] = $GNB_MEANS;
const float GNB_STDEVS[LDA_CLASSES][LDA_DIMENSIONS] = $GNB_STDEVS;

#endif